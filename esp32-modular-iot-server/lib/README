# Private PlatformIO Libraries

This directory is reserved for project-specific PlatformIO libraries.

The current firmware keeps its application code in `src/`, with reusable hardware controllers under `src/devices/`. Add code here only when it should be built as a separate private library and reused across multiple firmware components.
