# Shared Include Directory

This PlatformIO directory is available for public project headers.

The active firmware currently keeps headers next to implementation files under `src/`, `src/devices/`, and `src/modules/`. Use this directory only for headers that should be shared broadly across the firmware without belonging to a specific module or device controller.
